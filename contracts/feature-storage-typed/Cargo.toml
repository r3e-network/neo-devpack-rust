[package]
name = "storage-typed-neo"
version = "0.13.1"
edition = "2021"
description = "Feature-coverage sample: NeoStorage + NeoStorageContext + syscall storage"
license = "MIT"

[lib]
crate-type = ["cdylib"]

[dependencies]
neo-devpack = { path = "../../rust-devpack", version = "0.13.1", default-features = false }

[features]
default = ["neo-devpack/std"]

[workspace]

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = "symbols"
