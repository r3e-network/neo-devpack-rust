[package]
name = "neo-vm-test"
version = "0.13.1"
edition = "2021"
license = "MIT"
description = "Run compiled Neo N3 contracts on a real NeoVM from Rust tests (e2e harness)"
publish = false

[dependencies]
serde_json = "1.0"

[workspace]
