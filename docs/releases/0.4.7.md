# Neo LLVM v0.4.7 Release Notes

Release date: 2026-02-11

## Highlights

- Promoted workspace and `wasm-neovm` package version from development suffix (`0.4.7-dev`) to formal release version `0.4.7`.
- Published a formal non-dev release cut for downstream consumers expecting stable semver metadata.

## What Changed

- Updated workspace package version metadata to `0.4.7`.
- Updated `wasm-neovm` dependency version pin in workspace dependencies to `0.4.7`.
- Synced lockfile package entries so `wasm-neovm` resolves as `0.4.7`.

## Verification

The following commands pass for this release:

- `cargo test -p wasm-neovm`
- `cargo test --manifest-path rust-devpack/Cargo.toml`
- `cargo clippy -p wasm-neovm --all-targets -- -D warnings`
- `cargo clippy --manifest-path rust-devpack/Cargo.toml --all-targets -- -D warnings`
