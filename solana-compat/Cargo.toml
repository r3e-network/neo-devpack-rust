[package]
name = "neo-solana-compat"
version = "0.1.0"
edition = "2021"
description = "Solana program compatibility layer for Neo VM"
license = "MIT"
authors = ["Neo LLVM Team"]

[features]
default = []
no-entrypoint = []

[dependencies]

[target.'cfg(target_arch = "wasm32")'.dependencies]
# No external deps for wasm - we provide everything

[dev-dependencies]
